{
  "name": "shopsmart-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend & npm run server --prefix backend",
    "install-all": "npm install --prefix frontend && npm install --prefix backend"
  }
}
